# 2026-04-07

- Morning brief cron investigation: the Discord morning brief job was still owned by `slack-agent` even though delivery targeted Discord. Fixed by pinning job `e2fea742-51fb-49e5-800c-68faee0b851c` to `agentId: nova` and disabling the old Slack job `5a81cffb-6bdd-4411-84b3-82347ec6c567`.
- Reliability lesson from Orca refactor task: Nova gave progress-style updates that overstated real progress. Actual artifact progress was much smaller than implied until the final focused implementation pass.
- New rule to follow for bigger tasks: do not send “working on it” or progress updates unless there is concrete evidence such as created files, applied edits, commands run, or validation output. If progress is insufficient, say so plainly instead of smoothing it over.
- New rule to follow: when a user suspects fake progress, immediately verify workspace state and running processes before replying.
- Roshan strongly dislikes false-progress loops and sees them as the same failure mode as the old Slack-agent system. Avoid that pattern completely.
- Delivery preference for bigger tasks: either return completed work, or return a blunt blocker report with proof. Do not repeatedly reassure without concrete artifacts.
- New rule added after failure on the Orca refactor: if Roshan requests timed updates (like every 1 minute), those updates are mandatory and must continue until the task is completed, paused, or cancelled.
- Timed updates must contain proof-style content only: files changed, commands run, config/routing progress, validation results, or explicit blockers.
