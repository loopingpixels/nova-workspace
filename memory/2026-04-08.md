# 2026-04-08

- Orca architecture lesson: the standalone Discord bot path was the wrong architecture for Roshan's expectation. Correct direction is an OpenClaw-managed `orca` agent with separate Discord account/routing and local trading wrappers, using OAuth/OpenClaw model path rather than direct `OPENAI_API_KEY`.
- Orca setup progress completed today: created OpenClaw agent `orca`, copied Nova OAuth auth profile into `/home/roshan/.openclaw/agents/orca/agent/auth-profiles.json`, set Orca identity in OpenClaw config, added separate Discord account `orca`, and bound `discord:orca` to the `orca` agent.
- Operational lesson reinforced: when Roshan asks for timed progress updates (for example every 1 minute), they are mandatory until the task is done, paused, or cancelled, and must be short plus proof-based.
- Reliability lesson reinforced: do not claim progress without concrete artifacts or validation. If progress is thin, say so plainly. If Roshan doubts work is happening, verify actual workspace/process state immediately before replying.
- Orca still had mixed-response issues after routing cleanup: at different points there were both standalone bot replies and OpenClaw-managed replies active. Need to verify process state before assuming duplicates are fixed.
- Orca conversational extension added in the work-item code: support for a non-trading conversational lane and identity/behavior update intent, with local profile persistence at `/home/roshan/.openclaw/workspace/work-items/orca/config/orca_profile.yaml`. Validation showed the profile file was created, loaded, and reused successfully.
- Roshan prefers commands given for terminal use as raw command-only messages with no explanation in the same message.
- Family language note: in stories or family context, Roshan may be called "Thaththi" (Dad in Sinhala), and Mum is often called "Mimi" by the kids because Leon originally pronounced "mum" that way and Maylee copied it.
