# 2026-04-09

- Orca routing lesson reinforced: Roshan means the OpenClaw-managed `orca` agent/session, not the standalone Python Discord bot in `/home/roshan/.openclaw/workspace/work-items/orca/project/orca_discord_bot.py`.
- Critical mistake today: asking Roshan to run `./start_orca.sh` restarted the standalone Python Discord bot and created duplicate replies again (robotic local bot + OpenClaw-managed Orca).
- Future rule: do not tell Roshan to start/restart the standalone Orca bot when the task is about the live Discord DM Orca agent. First verify which runtime is serving replies via `openclaw status`, process list, and routing config.
- If implementation is requested for Orca Discord behavior, default to the OpenClaw-managed Orca path unless Roshan explicitly asks for the standalone local wrapper.
# 2026-04-09

- Orca chart delivery investigation outcome: the chart generation itself works, but the live OpenClaw-managed Orca Discord issue was mainly reply-format related. OpenClaw outbound media expects `MEDIA:<path-or-url>` or the message tool, not literal `<attachment path="..." />` text.
- Durable lesson: for Discord/OpenClaw image sends, prefer `MEDIA:./relative/path.png` or the message tool path; avoid inventing XML-like attachment syntax and avoid assuming file:// URLs will help.
- Reliability lesson reinforced again: when Roshan asks if work is still happening, verify actual file/process/workspace state before replying. Do not imply ongoing progress without fresh evidence.
- Roshan explicitly wants autonomous continuation on fixes until done; do not idle waiting for repeated nudges when there is a clear next engineering step.
- When fixing an issue, default to resolving discovered blockers/problems autonomously instead of stopping to report each blocker. Prioritize continuing the fix unless a decision is truly sensitive, destructive, or externally impactful.
- New reliability rule agreed with Roshan: before any in-progress status update, verify actual evidence first (git diff/status, file mtimes, logs, process state, validation output). If there is no fresh evidence, say there is no real progress instead of implying work is happening.
