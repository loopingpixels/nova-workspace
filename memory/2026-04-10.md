- Orca live Discord chart fix work on 2026-04-10: confirmed real blocker was the live OpenClaw Orca runtime environment, not the old standalone Python bot path.
- Created local chart script in Orca workspace: `/home/roshan/.openclaw/workspace/work-items/orca/scripts/generate_local_chart.py`.
- Created local Orca chart virtualenv `.chart-venv` and installed chart/runtime deps there after system Python user install was blocked by externally-managed environment.
- Confirmed local chart generation now works in the Orca workspace runtime and outputs `./reports/charts/aapl_daily_chart.png` with AAPL EMA values.
- Final outcome: Roshan confirmed the live Discord Orca route is now working correctly end-to-end for chart requests.
- Important lesson: when fixing OpenClaw-managed agents, verify the actual live runtime/session path first; do not assume older standalone bot/module paths are in use.
- Roshan wants strict proof-first execution rules applied when he gives real tasks to complete: do the work, avoid fake progress, and only claim done with evidence. For casual conversation, normal natural replies are fine.
- Roshan wants a twice-daily autonomous review system: once mid-day and once end-of-day, review recent sessions for new workflows/rules/tracked items, update memory/logs/workflows, and sync durable changes to GitHub.
- Wrapup plugin debugging on 2026-04-10: `/home/roshan/.openclaw/workspace-nova/plugins/wrapup/index.js` was updated to force `agentId: 'nova'`, `workspaceDir: '/home/roshan/.openclaw/workspace-nova'`, `provider: 'openai-codex'`, `model: 'gpt-5.4'`, and auth profile `openai-codex:roshan2008web@gmail.com` for embedded `/wrapup` runs.
- Durable lesson: embedded `runEmbeddedPiAgent(...)` resolution depends on `agentId` for workspace/model/auth directory selection; forcing provider/auth alone is not enough if runtime still inherits the wrong agent context.
- Added a reusable slash-command scaffold under `plugins/command-factory/` with templates plus `create-command.mjs` so new custom OpenClaw commands can be generated quickly and consistently.
- Generated a first concrete command plugin under `plugins/mayleo/` for `/mayleo`, which runs an embedded Nova agent in the Nova workspace to write/publish Mayleo Tales posts from a short description and attached images.
- Durable workflow update: for future custom slash commands, prefer generating from `plugins/command-factory` so embedded commands inherit the known-good Nova agent/workspace/auth context instead of hand-rolling each plugin from scratch.
- Nova startup/briefing investigation on 2026-04-10: Roshan reported a missing morning brief. Confirmed the morning brief is delivered by cron, not heartbeat.
- Durable config outcome: morning brief schedule was changed from weekdays-only to daily at 5:15 AM Pacific/Auckland.
- Durable debugging lesson: a missing scheduled brief may be due to no scheduled run on that day (e.g. weekday-only cron on Saturday), not runtime failure.
- Nova heartbeat issue on 2026-04-10: CLI enable appeared to succeed but live status still showed heartbeat disabled and heartbeat last/run state was inconsistent (`empty-heartbeat-file` despite populated HEARTBEAT.md).
- Durable fix for Nova heartbeat: add explicit per-agent heartbeat config rather than relying on ambiguous global toggle/state; after restart, live status showed `30m (nova)`.
- Slack-agent/Gmail investigation on 2026-04-10: recurring log noise tied to Gmail hooks showed sessions like `agent:slack-agent:hook:gmail:*`, even though Roshan is not actively using slack-agent.
- Durable config cleanup: patched hooks to route Gmail ingress to Nova instead of slack-agent by setting `hooks.defaultSessionKey = agent:nova:hook:gmail` and `hooks.allowedAgentIds = ["nova"]`.
- Durable runtime lesson: Gmail hook bind errors were caused by a webhook watcher process already owning `127.0.0.1:8788`; verify actual owner/runtime before assuming hook config is wrong.
- After restart/cleanup on 2026-04-10, live processes showed current webhook stack owning port 8788 (`openclaw`, `openclaw-webhooks`, and `gog gmail watch serve`), suggesting the old orphaned Apr 9 watcher was replaced by the fresh runtime.
- Roshan explicitly wants proof-based status updates when asked whether work is actually happening; when he questions progress, answer with concrete config/process/runtime evidence.
